<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>cn.erectpine</groupId>
    <artifactId>wls-cloud</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>wls-cloud</name>
    <description>wls起步 -- 微服务聚合工程</description>

    <properties>
        <!--项目版本-->
        <java.version>1.8</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!--主要框架-->
        <spring.boot.version>2.4.1</spring.boot.version>
        <spring.cloud.version>2020.0.0</spring.cloud.version>
        <spring.cloud.alibaba.version>2.2.4.RELEASE</spring.cloud.alibaba.version>
        <!--数据库连接框架-->
        <mybatis.plus.version>3.4.1</mybatis.plus.version>
        <druid.version>1.2.4</druid.version>
        <!--工具类-->
        <hutool.version>5.5.7</hutool.version>
        <fastjson.version>1.2.74</fastjson.version>
        <validation.api.version>2.0.1.Final</validation.api.version>
        <hibernate-validator.version>6.0.17.Final</hibernate-validator.version>
        <freemarker.version>2.3.30</freemarker.version>
        <pagehelper.version>1.2.5</pagehelper.version>
        <!--微服务聚合-->
        <wls.system.version>0.0.1-SNAPSHOT</wls.system.version>
        <!--通用模块聚合-->
        <wls.common.version>0.0.1-SNAPSHOT</wls.common.version>
        <wls.common.core.version>0.0.1-SNAPSHOT</wls.common.core.version>
        <wls.common.gencode.version>0.0.1-SNAPSHOT</wls.common.gencode.version>
        <wls.common.log.version>0.0.1-SNAPSHOT</wls.common.log.version>
        <wls.common.redis.version>0.0.1-SNAPSHOT</wls.common.redis.version>
        <wls.common.web.version>0.0.1-SNAPSHOT</wls.common.web.version>
    </properties>

    <modules>
        <module>wls-system</module>
        <module>wls-common</module>
        <module>wls-gateway</module>
        <module>wls-dict</module>
    </modules>
    <packaging>pom</packaging>

    <dependencyManagement>
        <dependencies>
            <!-- spring-boot的依赖配置-->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- spring-cloud的依赖配置-->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring.cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- spring-cloud-alibaba的依赖配置-->
            <dependency>
                <groupId>com.alibaba.cloud</groupId>
                <artifactId>spring-cloud-alibaba-dependencies</artifactId>
                <version>${spring.cloud.alibaba.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!--阿里数据库连接池 -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>
            <!--mybatis-plus-->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-boot-starter</artifactId>
                <version>${mybatis.plus.version}</version>
            </dependency>

            <!--参数验证-->
            <dependency>
                <groupId>javax.validation</groupId>
                <artifactId>validation-api</artifactId>
                <version>${validation.api.version}</version>
            </dependency>
            <!-- 验证框架 -->
            <dependency>
                <groupId>org.hibernate.validator</groupId>
                <artifactId>hibernate-validator</artifactId>
                <version>${hibernate-validator.version}</version>
            </dependency>

            <dependency>
                <groupId>cn.erectpine</groupId>
                <artifactId>wls-common-core</artifactId>
                <version>${wls.common.core.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.erectpine</groupId>
                <artifactId>wls-common-gencode</artifactId>
                <version>${wls.common.gencode.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.erectpine</groupId>
                <artifactId>wls-common-log</artifactId>
                <version>${wls.common.log.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.erectpine</groupId>
                <artifactId>wls-common-redis</artifactId>
                <version>${wls.common.redis.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.erectpine</groupId>
                <artifactId>wls-common-web</artifactId>
                <version>${wls.common.web.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!--lombok-->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <!--热启动依赖-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
    </dependencies>


    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
